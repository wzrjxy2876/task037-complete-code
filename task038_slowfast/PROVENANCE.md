# Task038 provenance

Task037 is authoritative for Dynamic3D signed temporal semantics, robust
D_abs normalization, Schur-complement D_rel, global standardized BMS, signed
functional similarity, active-demand coverage, float32 Delta_total, ordinal
percentile ranks, and dynamic F3 ordering.

The legacy SlowFast files are authoritative only for the
slowfast_16x8_resnet101_kinetics400 topology, stage names, fusion wiring,
checkpoint naming, and the existing dataset/training mechanism. Legacy
InteractionPruner science is archive-only: no old weighted amplitude formula,
cross-layer impact, per-layer clustering, manifold score, heuristic split,
threshold admission, or physical shrinking is used.

All Task038 candidate units are Conv3d output channels. Analytical costs include
the output filter, affine BN gamma/beta, and a tied downsample output filter/BN
when present. Consumer input weights and BN running statistics are excluded.
